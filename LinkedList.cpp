#include <bits/stdc++.h>

using namespace std;
using ll = long long;

template<typename T>

class Node {

	T data;
	Node<T> *next;
public:

	Node(T data)
	{
		this->data = data;
		next = NULL;
	}
};


class LinkedList {

}

int main()
{
	ios_base :: sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);



	return 0;
}