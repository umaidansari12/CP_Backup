#include <bits/stdc++.h>

using namespace std;
using ll = long long;

int main()
{
	ios_base :: sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);
	int t = 1;
	//cin>>t;
	while (t--) {
		int n;
		cin >> n;
		/*for (int i = 1; i <= n; i++) {
			if ((n % i) == 0) {
				cout << i << "\n";
			}
		}*/
		/*for (int i = 1; i * i <= n; i++) {
			if ((n % i) == 0) {
				cout << i << "\n";
			}
		}*/

		cout << n << endl;
	}

	return 0;
}