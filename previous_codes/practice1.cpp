#include <bits/stdc++.h>

using namespace std;
using ll = long long;

int main()
{
	ios_base :: sync_with_stdio(false);
	/*	cin.tie(NULL);
		cout.tie(NULL);*/
	int T;
	cin >> T;
	while (T--)
	{
		double n;
		int x;
		cin >> n >> x;
		int t = n;
		if (n < 0)
			n = n * (-1);
		double pro = 1.0;
		for (int i = 0; i < n; i++)
		{
			prox;
		}
		if (t < 0)
			pro = (double)1.0 / (double)(pro);
		return pro;
	}


	return 0;
}